import { style } from "@vanilla-extract/css";

export const container = style({
  display: "flex",
  gap: "16px",
  alignItems: "center",
  marginBottom: "16px",
});

export const link = style({
  textDecoration: "none",
});
